"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault");

var _Object$defineProperty = require("@babel/runtime-corejs3/core-js-stable/object/define-property");

_Object$defineProperty(exports, "__esModule", {
  value: true
});

exports.default = numberCeil;

var _concat = _interopRequireDefault(require("@babel/runtime-corejs3/core-js-stable/instance/concat"));

var _parseInt2 = _interopRequireDefault(require("@babel/runtime-corejs3/core-js-stable/parse-int"));

function numberCeil(count, text) {
  var _context;

  if (!count) {
    return '';
  }

  if (typeof count == 'string') {
    count = (0, _parseInt2.default)(count, 10);
  }

  var baseNum = 10000;

  if (count < baseNum) {
    return count + (text || '');
  }

  var result = count / baseNum;
  var integer = Math.floor(result);
  var decimals = Math.ceil((result - integer) * 10);

  if (decimals == 10) {
    ++integer;
    decimals = 0;
  }

  return (0, _concat.default)(_context = "".concat(integer, ".")).call(_context, decimals, "\u4E07");
}