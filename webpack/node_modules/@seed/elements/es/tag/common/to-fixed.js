import _Array$from from "@babel/runtime-corejs3/core-js-stable/array/from";
import _concatInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/concat";

/**
 * @since 2018-11-29 15:48
 * @author vivaxy
 */
export default function toFixed(number, length) {
  if (number >= Math.pow(10, length) || number < 0) {
    var _context;

    throw new Error(_concatInstanceProperty(_context = "Error number `".concat(number, "` to fixed length ")).call(_context, length, "."));
  }

  var numberString = String(number);
  return _Array$from({
    length: length - numberString.length
  }, function () {
    return 0;
  }) + numberString;
}