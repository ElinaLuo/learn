import _concatInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/concat";
import _parseInt from "@babel/runtime-corejs3/core-js-stable/parse-int";
export default function numberCeil(count, text) {
  var _context;

  if (!count) {
    return '';
  }

  if (typeof count == 'string') {
    count = _parseInt(count, 10);
  }

  var baseNum = 10000;

  if (count < baseNum) {
    return count + (text || '');
  }

  var result = count / baseNum;
  var integer = Math.floor(result);
  var decimals = Math.ceil((result - integer) * 10);

  if (decimals == 10) {
    ++integer;
    decimals = 0;
  }

  return _concatInstanceProperty(_context = "".concat(integer, ".")).call(_context, decimals, "\u4E07");
}