import React, { Fragment } from 'react';
import { prefixCls } from '@seed/ui-base';
import '../style/index.less';

export default class FixedBottomContainer extends React.Component {
  render() {
    return (
      <Fragment>
        <div className={prefixCls('fixed-bottom-container')}>
          {this.props.children}
        </div>
        <div className={prefixCls('fixed-bottom-block')} />
      </Fragment>
    );
  }
}
