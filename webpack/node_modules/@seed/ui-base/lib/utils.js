"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault");

var _Object$defineProperty = require("@babel/runtime-corejs3/core-js-stable/object/define-property");

_Object$defineProperty(exports, "__esModule", {
  value: true
});

_Object$defineProperty(exports, "NOOP", {
  enumerable: true,
  get: function get() {
    return _const.NOOP;
  }
});

exports.priceZeroClear = exports.prefixCls = void 0;

var _slicedToArray2 = _interopRequireDefault(require("@babel/runtime-corejs3/helpers/slicedToArray"));

var _const = require("./const");

// bem class拼接规则
var prefixCls = function prefixCls() {
  var temp = _const.PREFIX_CLS + '-'; // debugger
  // const [block,element,modifier] = [...args];
  // const [b,e,m] = [...args];
  // console.log([...args]);
  // const [block,element,modifier] = [...arguments];

  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  temp = temp + args.join('-'); // if(b){
  //     temp = temp + b;
  // }
  // if(e){
  //     temp = temp + "-" +e;
  // }
  // if(m){
  //     temp = temp + "-" +m;
  // }
  //

  return temp;
}; // 小数尾部清0


exports.prefixCls = prefixCls;

var priceZeroClear = function priceZeroClear(notclearZero, originPrice) {
  if (notclearZero === true) return originPrice; //不移除价格小数末尾的0

  if (!originPrice) return '';
  var changedPrice = originPrice + '';

  var _changedPrice$split = changedPrice.split('.'),
      _changedPrice$split2 = (0, _slicedToArray2.default)(_changedPrice$split, 2),
      intStr = _changedPrice$split2[0],
      miniStr = _changedPrice$split2[1];

  if (!!miniStr) {
    var miniStrNext = miniStr.replace(/(0+$)/, '');

    if (!!miniStrNext) {
      return intStr + '.' + miniStrNext;
    }

    return intStr;
  } else {
    return changedPrice;
  }
};

exports.priceZeroClear = priceZeroClear;