import { prefixCls, priceZeroClear } from '../src/utils';

test('ImageDisplay', function() {
  expect(prefixCls('module', 'component')).toBe('seed-module-component');
  expect(priceZeroClear).toBeDefined();
});
